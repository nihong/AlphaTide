# 🤖 AGENTS.md: AI 投资哨兵 (Automated Sentinel)

## 🌟 项目定义
**AlphaTide** 已升级为一个**全自动每日监听与决策系统**。它不再仅仅是一个查询工具，而是一个能够自我调度、多维验证、并最终生成决策建议的 AI 投研大脑。

## 🎯 AI 智能体使命 (System Prompt)
作为 **AI 投资哨兵**，您必须严格执行以下多维过滤闭环：
1.  **大盘红绿灯**：基于沪深300指数与均线关系，决定仓位基调。
2.  **动态潮汐定位**：锁定资金流入但价格未引爆的“蓄势板块”。
3.  **四维个股精选**：
    - **财务面**：ROE > 15%，净现比 > 1.0，合同负债正增长。
    - **强度面** (RS)：个股表现必须强于大盘 (Relative Strength > 0)。
    - **技术面**：价格站稳 MA20 均线，排除趋势向下的个股。
    - **风险面**：自动扫描**双轨卖出信号**（趋势破坏 vs 情绪过热）。

---

## 🏗️ 核心模块架构

### 1. 监控指挥部 (`src/market_monitor.py`)
- 总调度，串联大盘判断、行业锁定及个股精选。

### 2. 风险管理引擎 (`src/risk_manager.py`)
- **RS 强度计算**：确保选出的股票是“强中之强”。
- **三轨卖出预警防线**：
    - **绝对防线 (Trailing Stop)**：股价从近期高点回撤超 10%，触发生存红线，无条件离场。
    - **激进型 (Aggressive)**：价格跌破 MA20 均线，趋势破坏，立即预警。
    - **保守型 (Conservative)**：乖离率过大 (BIAS > 15%)，情绪过热，提示减仓。

### 3. 数据稳固层 (`src/data_fetcher.py`)
- **本地缓存机制**：财报数据 24 小时缓存，行情数据 4 小时缓存，极大降低被封风险。
- **反爬策略**：内置随机延迟与 User-Agent 随机切换，模拟真实人类行为。

---

## 🔐 安全与隐私
- **API Key 管理**：请将 `DEEPSEEK_API_KEY` 写入根目录的 `.env` 文件。
- **隔离保护**：`.env`、`.cache/`、`history/` 及 `reports/` 已加入 `.gitignore`，**永远不会**提交到公共仓库。
- **模板参考**：查看 `.env.example` 进行快速配置。


## ⏰ 自动化运行 (OpenClaw / QClaw)
建议将本项目集成至 **OpenClaw** 或 **QClaw** 平台。
- **使命**：实现无人值守的每日扫描与实时预警推送。
- **触发逻辑**：交易日 16:00 自动执行 `python3 main.py --auto`。
- **通知渠道**：通过 QClaw 插件将生成的 Markdown 报告推送至用户移动端。

---
*最后更新: 2026-05-28 | 重点: 强化安全隔离 & 自动化部署指南*
��*
