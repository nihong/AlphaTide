# 🤖 AGENTS.md: AlphaTide V4.5 - AI 驱动的逻辑共振与资金追随系统

## 🌟 项目终极定义
**AlphaTide V4.5** 已经彻底从“纯量价/技术指标盲测”的石器时代，跨越到了对标华尔街最前沿的 **“主观逻辑 AI 化（AI-Driven Fundamental Quant）”** 时代，并引入了机构级**无杠杆宏观融券对冲（Macro Hedge）**。
量化的核心不再是算 K 线均线，而是**算逻辑的硬度**、**算资金的投票**与**算大盘的牛熊**。

## 🎯 系统宏观哲学 (The Alpha Philosophy)
*   **不迷信指标，只相信逻辑与真金白银**：任何单纯因为指标超跌而买入的行为都是在接飞刀。
*   **买入公式** = `(核心逻辑源发) + (另类物理数据证实) + (聪明资金压倒性流入) + (右侧技术面突破)`
*   **风控公式** = `(宏观天气骤变) OR (逻辑被证伪) OR (物理止损红线触发)`
*   **对冲公式** = `(大盘跌破60日均线) -> (清仓多头) + (1x杠杆等额融券做空 CSI 300)`

---

## 🏗️ 全新四大核心引擎架构

### 模块 1: 🧠 AI 投研大脑 (Idea Generation & 动态建仓)
*文件映射：`src/brain_nlp.py`*
*   **职能**：解决“买什么（逻辑起点）”。每天作为侦察兵，自动阅读海量非结构化文本。
*   **数据源**：顶流券商研报（如天风、中信）、发改委政策文件、行业财报、全球产业节点新闻。
*   **核心逻辑**：利用 DeepSeek 等大语言模型提纯“长跨度产业趋势”或“反转预期”，精准提取核心标的，并打上“逻辑标签”。
*   **输出**：维护一个包含 30-50 只高潜标的的**动态自选池（Watchlist）**。

### 模块 2: 🕵️ 多维证伪过滤器 (The Truth Filter)
*文件映射：`src/falsification_filter.py`*
*   **职能**：解决“防忽悠（逻辑去伪存真）”。绝不轻信研报的单方面吹嘘。
*   **宏观与行业物理探针**：
    *   *航运/周期逻辑* -> 必须验证 BDI/CCFI 指数是否拐头向上。
    *   *大宗/农业逻辑* -> 必须验证生猪价格、LME 铜价是否触底反弹。
    *   *科技/出海逻辑* -> 必须验证中美利差、费城半导体指数（SOX）、出口数据。
*   **输出**：物理数据不支持的标的，直接从自选池中无情剔除（Veto）。

### 模块 3: 💸 全市场资金投票器 (Capital Flow Voting)
*文件映射：`src/capital_flow_voter.py`*
*   **职能**：解决“什么时候买（资金面确认）与大盘牛熊判定”。逻辑再好，资金不认也是白搭。
*   **板块共振与牛熊探测**：个股上涨是否伴随整个赛道的集体异动？大盘（沪深300）是否跌破 60 日生命线？
*   **聪明资金追踪**：主力大单净流入、底部是否出现堆量突破（Volume > 1.5x MA20）？
*   **跨市场验证**：A股 AI 推荐标的必须得到美股科技股大盘趋势的支撑。

### 模块 4: ⚔️ 冷血执行官 (Execution & Risk Management)
*文件映射：`src/execution_engine.py`*
*   **职能**：解决“右侧买入、保命保本（回撤控制）与宏观做空”。
*   **狙击买入点**：当且仅当自选池中的标的满足资金投票，且突破关键短期阻力位（右侧突破）时才拔枪射击。
*   **多维逃顶卖出与对冲**：
    1.  **物理防弹衣（绝对止损）**：无情的 ATR 移动止损线（如最高点回撤 15%）。跌停板强锁识别，触碰红线，机器强制斩仓。
    2.  **宏观对冲（Macro Hedge）**：当模块3判定进入 `BEAR`（熊市）时，停止一切做多，并自动开启 `SHORT` 模式，融券做空大盘宽基（如 601318 代理 CSI 300），**严格锁定 1x 杠杆，绝不放大风险**，并在空头趋势中应用逆向 ATR 止盈止损。

---

## 🔬 离线解耦回测框架 (Decoupled Backtesting)
V4.5 引入了极度专业的华尔街级别回测机制，彻底解决“大模型调用昂贵”与“未来函数”问题：
1. **`monte_carlo_backtest.py` (猴子盲测)**: 拔掉 AI 大脑，全市场随机挑股硬塞给风控引擎，测试系统的物理装甲下限（实测最大回撤 < 10%）。
2. **`real_market_6yr_backtest.py` (6年长牛熊回测)**: 预先写入 2020-2026 年的核心宏观主线（后视镜 AI），测试资金投票器和执行引擎的抓取率与逃顶率（实测年化收益 5.15%，最大回撤 12%）。
3. **`recent_60d_backtest.py` (近 60 天极限极速实战)**: 测试短期内高波动环境下的止盈止损与空头切换。

## ⏰ 自动化运行流 (The V4.5 Pipeline)
每天收盘后（或开盘前），系统自动拉起编排任务：
1. `brain_nlp.py` -> 爬取最新研报，生成逻辑，更新 Watchlist。
2. `falsification_filter.py` -> 拉取 Akshare 宏观与另类数据，清洗 Watchlist。
3. `capital_flow_voter.py` -> 计算幸存标的的资金面打分，并判定宏观牛熊。
4. `execution_engine.py` -> 根据得分、宏观状态和右侧技术信号，生成实盘做多/做空指令。

*最后更新: 2026-06-06 | 战略升级: V4.5 正式上线 Macro Hedge (无杠杆融券对冲) 与全体系解耦回测框架。*
