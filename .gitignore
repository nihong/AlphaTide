# 核心隐私：绝对不上传
.env

# 临时缓存：建议不上传，保持仓库整洁
.cache/
__pycache__/
*.py[cod]

# 运行数据：按照您的要求，允许上传（如果不想要了，可以再加回来）
# reports/
# history/

# 环境文件夹
venv/
.venv/
.antigravitycli/

# Scratch and temporary caches
scratch/
*.json
!history/data.json
